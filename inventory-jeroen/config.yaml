name: Inventory Jeroen
slug: inventory_jeroen
description: Materiaal/voorraadlijst met uitbreidbare types, afmetingen en locaties
version: "1.0.0"
url: https://github.com/jlagendijk/inventory
arch: [amd64, aarch64, armv7]
startup: services
init: false

ingress: true
ingress_port: 8101
panel_icon: mdi:toolbox
panel_title: Inventory Jeroen

ports:
  8101/tcp: null

options:
  db_host: core-mariadb
  db_port: 3306
  db_user: inventory_user
  db_password: change_me
  db_name: inventory_jeroen
  base_url: ""

schema:
  db_host: str
  db_port: int
  db_user: str
  db_password: password
  db_name: str
  base_url: str

map:
  - share:rw

hassio_api: true
homeassistant_api: true
backup: cold
